% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{plot_normalization}
\alias{plot_normalization}
\title{Molecular normalization boxplot (gene-level logFC vs reference)}
\usage{
plot_normalization(
  norm,
  reference_label = "Non-Lesional",
  y_label = NULL,
  title = NULL,
  reg_palette = .NORM_REG_COLORS,
  arm_strip = .NORM_ARM_STRIP,
  box_width = 0.7,
  week_labels = TRUE
)
}
\arguments{
\item{norm}{The \code{normalization} table from \code{\link[=compute_normalization]{compute_normalization()}}.}

\item{reference_label}{Short reference name for the y-axis, e.g.
\code{"Non-Lesional"} or \code{"Controls"} -> \code{"logFCH (vs. <label>)"}.}

\item{y_label}{Full y-axis title (overrides \code{reference_label} when given).}

\item{title}{Plot title.}

\item{reg_palette}{Named colours for Down / Up (default blue / red).}

\item{arm_strip}{Named fill colours for the treatment facet strips
(default Drug = darkgreen, Placebo = darkorange).}

\item{box_width}{Width of the (dodged) boxes.}

\item{week_labels}{When \code{TRUE}, relabel \code{"W4"} as \code{"Week 4"} on the x-axis
(the underlying data are untouched).}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws the normalization figure: per visit (baseline included) the
distribution of gene-level log-fold changes of lesional tissue against the
healthy reference, faceted by treatment arm. Boxes are filled by baseline
regulation (Down = blue, Up = red) and the mean of each box is a
white-bordered filled circle; a dashed line marks the healthy reference at 0.
Treatment facet strips are coloured (Drug = darkgreen, Placebo = darkorange)
via \code{\link[ggh4x:facet_grid2]{ggh4x::facet_grid2()}}; a single arm drops the facet.
}
\examples{
\donttest{
df <- expand.grid(SubjectID = paste0("S", 1:6),
                  Visit = c("BL", "W4"), Tissue = c("LS", "NL"),
                  Gene = paste0("G", 1:8), stringsAsFactors = FALSE)
set.seed(1)
df$Value <- rnorm(nrow(df)) + ifelse(df$Tissue == "LS" & df$Visit == "BL", 2, 0)
nz <- compute_normalization(df, reference = "NL", degs = paste0("G", 1:8))
plot_normalization(nz$normalization, reference_label = "Non-Lesional")
}
}
\seealso{
\code{\link[=compute_normalization]{compute_normalization()}}, \code{\link[=evaluate_normalization]{evaluate_normalization()}}
}
