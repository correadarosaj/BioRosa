% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FancyAnnotatedHeatmap_vs2.R
\name{FancyAnnotatedHeatmap_vs2}
\alias{FancyAnnotatedHeatmap_vs2}
\title{Annotated heatmap with a bordered, high-resolution side table (v2)}
\usage{
FancyAnnotatedHeatmap_vs2(
  grafname,
  matx,
  annot.col,
  colsplitx = NULL,
  column_ha = NULL,
  BigTab = NULL,
  wdt = NULL,
  hgt = NULL,
  setname = "",
  column_names_max_height = unit(8, "cm"),
  BigTabcols = NULL,
  cfx = NULL,
  fdx = NULL,
  ShowColumnNames = FALSE,
  fnt_size_title = 20,
  scalerows = TRUE,
  rampvalues = 1.5,
  bottom_annotation = NULL,
  border = TRUE,
  cell_fun = NULL,
  table_border_col = NA,
  table_border_lwd = 1.2,
  table_header_fontsize = 9,
  table_header_fontface = 2,
  table_header_fill = "grey95",
  table_header_border_col = NA,
  table_header_border_lwd = 1.2,
  table_header_pad_mm = 1.5,
  table_header_height_mm = 14,
  table_body_fontsize = 10,
  table_body_fontface = 2,
  gene_col_fontsize = 10,
  gene_col_fontface = 2,
  table_cell_pad_spaces = 2,
  heatmap_column_width_cm = 0.6,
  table_col_width_cm = NULL,
  ppt_slide_width = 13.33,
  ppt_slide_height = 7.5,
  rows_per_inch = 10,
  ...
)
}
\arguments{
\item{grafname}{Output PDF path.}

\item{matx}{Numeric matrix of expression values (rows = genes, columns = samples).}

\item{annot.col}{Sample annotation data frame whose row names match `colnames(matx)`.}

\item{colsplitx}{Optional vector used to split heatmap columns into groups.}

\item{column_ha}{Optional `HeatmapAnnotation` placed at the top of the heatmap.}

\item{BigTab}{Optional big table of contrast statistics (passed through for sub-setting).}

\item{wdt, hgt}{PDF width / height (inches). When `NULL` (the default) the
PDF is sized to drop directly into a 16:9 PowerPoint slide
(13.33in x 7.5in): `wdt` defaults to `ppt_slide_width` and `hgt` scales
with the number of genes at `rows_per_inch` rows per inch, floored at
35\% of the slide height and capped at `ppt_slide_height`.}

\item{setname}{Caption shown below the heatmap.}

\item{column_names_max_height}{Max height for column-name labels.}

\item{BigTabcols}{Optional character vector; columns of `BigTab` whose names match are kept.}

\item{cfx}{Matrix of log fold-changes (rows = genes, columns = contrasts).}

\item{fdx}{Matrix of p-values or FDRs aligned to `cfx`.}

\item{ShowColumnNames}{Logical; show heatmap column labels.}

\item{fnt_size_title}{Font size for the heatmap column-split titles.}

\item{scalerows}{Logical; if `TRUE` z-score rows before plotting.}

\item{rampvalues}{Symmetric limit of the z-score colour ramp.}

\item{bottom_annotation}{Optional `HeatmapAnnotation` placed at the bottom.}

\item{border}{Logical; pass-through to `ComplexHeatmap::Heatmap`.}

\item{cell_fun}{Cell-drawing function for the heatmap body. Defaults to
`NULL` so `ComplexHeatmap::Heatmap` renders the colour ramp normally.
Pass a function (e.g. one that fills cells with white) to override.}

\item{table_border_col}{Colour of the rectangular border drawn around each
side-table column. Defaults to `NA` (no column border); set to a colour
such as `"black"` to draw outlines.}

\item{table_border_lwd}{Line width of the side-table column border (only
used when `table_border_col` is not `NA`).}

\item{table_header_fontsize}{Font size of each side-table column title (was 10 in v1).}

\item{table_header_fontface}{Font face of the side-table column title (1 = plain, 2 = bold).}

\item{table_header_fill}{Optional fill colour for the title box above each
side-table column. Use `NA` for no fill.}

\item{table_header_border_col}{Border colour for the title box. Defaults to
`NA` (no border); set to a colour to enable.}

\item{table_header_border_lwd}{Line width for the title box border (only
used when `table_header_border_col` is not `NA`).}

\item{table_header_pad_mm}{Vertical padding (mm) between the heatmap and the title box.}

\item{table_header_height_mm}{Height (mm) of the title box.}

\item{table_body_fontsize}{Font size of the side-table cell values (was 10 in v1).}

\item{table_body_fontface}{Font face of the side-table cell values.}

\item{gene_col_fontsize}{Font size of the leading gene-name column.}

\item{gene_col_fontface}{Font face of the leading gene-name column.}

\item{table_cell_pad_spaces}{Number of non-breaking spaces (`&nbsp;`) added
to the left and right of every side-table cell value so text does not
press against the column border. Increase for more breathing room.}

\item{heatmap_column_width_cm}{Width (cm) of each individual heatmap body
column. Decrease to make the heatmap body thinner so the side table gets
relatively more space.}

\item{table_col_width_cm}{Explicit width (cm) for each contrast column in
the side table. `NULL` lets ComplexHeatmap auto-size from the text width.}

\item{ppt_slide_width}{Default PDF width (inches) when `wdt` is `NULL`.
Default `13.33` matches a 16:9 PowerPoint slide. Use `10` for legacy
4:3 slides.}

\item{ppt_slide_height}{Default PDF height (inches) when `hgt` is `NULL`.
Default `7.5` matches a 16:9 PowerPoint slide.}

\item{rows_per_inch}{Target row density (rows per inch) used to auto-size
`hgt`. Higher values pack rows more tightly. Default `10`.}

\item{...}{Reserved for forward compatibility; currently ignored.}
}
\value{
Invisibly returns `NULL`; called for its side effect of writing a PDF to `grafname`.
}
\description{
Successor to [FancyAnnotatedHeatmap()] that renders the per-contrast side
table with explicit rectangular borders and larger, bolder header/body
fonts so the table reads clearly when the heatmap is exported at
publication resolution. Behaviour is otherwise compatible with
[FancyAnnotatedHeatmap()]; new visual parameters control the appearance of
the side table without changing the heatmap body.
}
\seealso{
[FancyAnnotatedHeatmap()], [doAnnotatedHeatmap()], [doTab()]
}
