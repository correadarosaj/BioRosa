% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doTrajectory.R
\name{doTrajectory}
\alias{doTrajectory}
\title{Plot per-patient trajectories of a clinical score over visits}
\usage{
doTrajectory(
  data,
  subject,
  time,
  score,
  group = NULL,
  time_levels = NULL,
  impute = c("none", "locf", "mice"),
  spaghetti = TRUE,
  group_mean = TRUE,
  error = c("none", "se", "sd", "ci"),
  facet = TRUE,
  palette = NULL,
  title = NULL,
  xlab = "Visit",
  ylab = NULL,
  conf.interval = 0.95,
  mice_m = 5,
  seed = 1970,
  file = NULL,
  width = 12,
  height = 6
)
}
\arguments{
\item{data}{A long-format data frame (one row per subject-visit).}

\item{subject}{Character. Column identifying the patient.}

\item{time}{Character. Column with the visit / time point.}

\item{score}{Character. Numeric column with the clinical score to plot.}

\item{group}{Character or \code{NULL}. Treatment/arm column. When supplied and
\code{facet = TRUE}, spaghetti panels are faceted by group.}

\item{time_levels}{Character vector giving the visit order. Defaults to the
factor levels of \code{time}, or its sorted unique values.}

\item{impute}{One of \code{"none"} (default), \code{"locf"} (last observation carried
forward), or \code{"mice"} (predictive-mean-matching multiple imputation,
averaged over \code{mice_m} completions). Imputation first completes the full
subject \eqn{\times} visit grid.}

\item{spaghetti}{Logical. Draw one line per patient. Default \code{TRUE}.}

\item{group_mean}{Logical. Overlay the group-mean trajectory. Default \code{TRUE}.}

\item{error}{One of \code{"none"} (default), \code{"se"}, \code{"sd"}, or \code{"ci"} — the
error bar drawn on the group mean. Computed with \code{\link[=summarySE]{summarySE()}}.}

\item{facet}{Logical. Facet the spaghetti by \code{group} (only when \code{spaghetti}
and \code{group} has more than one level). Default \code{TRUE}.}

\item{palette}{Optional colour vector. For spaghetti it colours patients; for
a mean-only plot it colours groups. Defaults to a distinct HCL palette.}

\item{title, xlab, ylab}{Plot title and axis labels. \code{ylab} defaults to \code{score}.}

\item{conf.interval}{Confidence level for \code{error = "ci"}. Default \code{0.95}.}

\item{mice_m}{Number of imputations when \code{impute = "mice"}. Default \code{5}.}

\item{seed}{Random seed for MICE reproducibility. Default \code{1970}.}

\item{file}{Optional path; if given the plot is written with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width, height}{Saved figure size in inches. Defaults \code{12} x \code{6}.}
}
\value{
A \code{ggplot} object (returned invisibly when \code{file} is written).
}
\description{
\code{doTrajectory()} draws publication-quality \strong{observed} longitudinal
trajectories of a clinical score: one coloured line per patient
("spaghetti"), an optional bold \strong{group-mean} line, and optional
\strong{error bars} (SE / SD / CI) on that mean. It is the descriptive companion
to \code{\link[=doTrajectoryModel]{doTrajectoryModel()}}, which plots model-estimated means with significance
stars.

Input is a \strong{long-format} data frame: one row per patient-visit, with
columns identifying the subject, the visit/time, the score, and (optionally)
the treatment group.
}
\examples{
\dontrun{
# Per-patient spaghetti + bold group mean, faceted by arm
doTrajectory(clin, subject = "record_id", time = "Visit",
             score = "EASI", group = "Trt",
             time_levels = c("W0", "W4", "W8", "W16"))

# Group mean +/- SE only (the classic mean-with-error-bars trajectory)
doTrajectory(clin, subject = "record_id", time = "Visit",
             score = "EASI", group = "Trt",
             spaghetti = FALSE, error = "se")
}
}
\seealso{
\code{\link[=doTrajectoryModel]{doTrajectoryModel()}} for mixed-model estimated means with
significance stars; \code{\link[=summarySE]{summarySE()}} for the error-bar summaries.
}
