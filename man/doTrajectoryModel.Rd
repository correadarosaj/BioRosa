% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doTrajectoryModel.R
\name{doTrajectoryModel}
\alias{doTrajectoryModel}
\title{Mixed-model estimated trajectories with significance stars}
\usage{
doTrajectoryModel(
  data,
  subject,
  time,
  score,
  group = NULL,
  baseline = NULL,
  time_levels = NULL,
  value = c("mean", "change", "pchange"),
  random = NULL,
  engine = c("lme", "lmer"),
  error = c("se", "ci"),
  impute = c("none", "locf", "mice"),
  reference = NULL,
  between = NULL,
  pvals = NULL,
  score_row = NULL,
  pval_prefix = "pvals_",
  baseline_label = "BL",
  star_breaks = c(-Inf, 0.001, 0.01, 0.05, 0.1, Inf),
  star_labels = c("***", "**", "*", "+", ""),
  palette = NULL,
  title = NULL,
  xlab = "Visit",
  ylab = NULL,
  conf.interval = 0.95,
  mice_m = 5,
  seed = 1970,
  file = NULL,
  width = 8.5,
  height = 5.5
)
}
\arguments{
\item{data}{A long-format data frame (one row per subject-visit).}

\item{subject, time, score}{Character. Columns for the patient, the visit, and
the numeric clinical score.}

\item{group}{Character or \code{NULL}. Treatment/arm column. Needed for between-arm
comparisons; within-arm-vs-baseline still works with a single arm.}

\item{baseline}{The visit treated as baseline for vs-baseline contrasts.
Defaults to the first level of \code{time_levels}.}

\item{time_levels}{Character vector giving the visit order. Defaults to the
factor levels / sorted unique values of \code{time}.}

\item{value}{What to plot on the y-axis: \code{"mean"} (estimated marginal mean,
the default), \code{"change"} (estimated change from baseline), or \code{"pchange"}
(estimated percent change from baseline).}

\item{random}{Optional random-effects specification. For \code{engine = "lme"} an
\pkg{nlme} formula (default \code{~ 1 | subject}); for \code{engine = "lmer"} a
parenthesised \pkg{lme4} term (default \code{(1 | subject)}).}

\item{engine}{\code{"lme"} (\pkg{nlme}, default) or \code{"lmer"} (\pkg{lme4}).}

\item{error}{\code{"se"} (default) or \code{"ci"} — error bars as mean +/- SE or as the
confidence interval.}

\item{impute}{One of \code{"none"} (default), \code{"locf"}, or \code{"mice"}; see
\code{\link[=doTrajectory]{doTrajectory()}}.}

\item{reference}{Reference arm for between-arm stars. Defaults to the first
level of \code{group}.}

\item{between}{Logical or \code{NULL}. Draw between-arm stars (vs \code{reference}).
\code{NULL} (default) enables them when \code{group} has more than one level.}

\item{pvals}{Optional override for the stars. Either a data frame with columns
\code{time} and \code{p.value} (optionally \code{group}, and \code{p.between}), \strong{or} a BigTab:
a data frame whose columns follow \verb{pvals_<group>.<time>vs<baseline_label>}
(see \code{pval_prefix}, \code{baseline_label}, \code{score_row}).}

\item{score_row}{When \code{pvals} is a BigTab, the row (gene/score) to read,
matched against row names or a \code{GENENAME} column.}

\item{pval_prefix}{Column prefix for BigTab p-values. Default \code{"pvals_"}.}

\item{baseline_label}{Token used for baseline in BigTab contrast suffixes
(e.g. \code{"BL"} in \code{pvals_Drug.W16vsBL}). Default \code{"BL"}.}

\item{star_breaks, star_labels}{Cutoffs and labels passed to \code{\link[=pval2star]{pval2star()}}.}

\item{palette}{Optional named colour vector for the arms.}

\item{title, xlab, ylab}{Title and axis labels. \code{ylab} defaults to a label
derived from \code{value}.}

\item{conf.interval}{Confidence level for \code{error = "ci"}. Default \code{0.95}.}

\item{mice_m, seed}{MICE controls; see \code{\link[=doTrajectory]{doTrajectory()}}.}

\item{file}{Optional path; if given the plot is written with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width, height}{Saved figure size in inches. Defaults \code{8.5} x \code{5.5}.}
}
\value{
A list with \verb{$plot} (a \code{ggplot}), \verb{$estimates} (the per-arm, per-visit
estimate table with \code{p_within} / \code{p_between} and the star labels), and
\verb{$model} (the fitted model).
}
\description{
\code{doTrajectoryModel()} fits a linear mixed-effects model to a longitudinal
clinical score and plots the \strong{model-estimated marginal means} per visit and
treatment arm, with error bars and \strong{significance stars}. Stars are computed
from the model by default (each visit vs. baseline within an arm, and,
optionally, between-arm difference-in-change), but can be \strong{overridden} with
p-values from a BigTab or a plain p-value table.

The model is a cell-means model, \code{score ~ group * time} with a per-subject
random intercept (the standard repeated-measures structure). Estimated means
and contrasts come from \pkg{emmeans}.
}
\details{
\strong{Within-arm stars} test each visit against \code{baseline} within an arm.
\strong{Between-arm stars} test, at each visit, the difference in change-from-
baseline between an arm and \code{reference} (a difference-in-differences
interaction contrast; for \code{value = "pchange"} the arms are compared directly
at each visit). Between-arm contrasts are best-effort: if \pkg{emmeans} cannot
form them they are skipped with a warning.

Stars use the \code{\link[=pval2star]{pval2star()}} convention: \verb{***} < 0.001, \verb{**} < 0.01,
\code{*} < 0.05, \code{+} < 0.1.
}
\examples{
\dontrun{
res <- doTrajectoryModel(clin, subject = "record_id", time = "Visit",
                         score = "EASI", group = "Trt", baseline = "W0",
                         value = "change", reference = "Pbo")
res$plot
res$estimates

# Override stars with p-values stored in a BigTab row:
res <- doTrajectoryModel(clin, subject = "record_id", time = "Visit",
                         score = "EASI", group = "Trt", baseline = "W0",
                         pvals = BigTab, score_row = "EASI")
}
}
\seealso{
\code{\link[=doTrajectory]{doTrajectory()}} for observed (non-model) trajectories.
}
