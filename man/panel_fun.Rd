% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel_fun.R
\name{panel_fun}
\alias{panel_fun}
\title{Minimal annotation-block panel function}
\usage{
panel_fun(index, levels)
}
\arguments{
\item{index}{Integer vector of column indices in the current block (supplied
by ComplexHeatmap; unused here).}

\item{levels}{The level label(s) of the current block, drawn as text.}
}
\value{
Called for its side effect of drawing into the current viewport;
  returns `NULL` invisibly.
}
\description{
`panel_fun()` is a simple panel function for `ComplexHeatmap::anno_block`: it
draws a bordered rectangle and prints the block's level label centred inside
it. Pass it as `anno_block(panel_fun = panel_fun, ...)`. It follows the
`panel_fun(index, levels)` signature ComplexHeatmap calls for each block.
}
\examples{
\dontrun{
ComplexHeatmap::anno_block(align_to = leveler(groups), panel_fun = panel_fun)
}
}
\seealso{
[leveler()]
}
